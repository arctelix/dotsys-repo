
# activate virtualenv
if [ "$PLATFORM_G" != "windows" ];then
    alias va='source bin/activate'
else
    alias va='Scripts\activate'
fi

# deactivate virtualenv
alias vd='deactivate'

# Django
alias pm='python manage.py'
alias pmrs='python manage.py runserver'